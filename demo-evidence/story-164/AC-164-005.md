# AC-164-005 Demo Evidence: BREAKING-Change Holdout-Sweep Protocol

**AC:** AC-164-005 (traces to PG-W72-BREAKING-HOLDOUT-SWEEP)  
**Story:** STORY-164 v1.10  
**Date:** 2026-07-11  
**Method:** Terminal captures — grep and head commands

---

## (a) CLAUDE.md Reference Row

```
$ grep -n -A1 "breaking-change-delivery-protocol" CLAUDE.md
250:| `.factory/maintenance/breaking-change-delivery-protocol.md` | BREAKING-change holdout-sweep obligation (PG-W72-BREAKING-HOLDOUT-SWEEP; `holdout-expectations-sweep: COMPLETE` required before PR for BREAKING or output-format-change stories) |
```

Row present at line 250, immediately after the `docs-writer-dispatch-guidance.md` row
added by AC-164-004 (line 249). The row wording matches the exact text specified in
AC-164-005(d).

---

## (b) Protocol Document — head -30

```
$ head -30 $REPO_ROOT/.factory/maintenance/breaking-change-delivery-protocol.md
# BREAKING-Change Holdout-Expectation Sweep Protocol

**Policy reference:** PG-W72-BREAKING-HOLDOUT-SWEEP
**Finding reference:** `.factory/cycles/wave-72/lessons.md` Lesson 2 (lines 46-79)
**Evidence story:** STORY-160 (wave-72, PR #389, squash 704fd2e)
**Codification story:** STORY-164 AC-164-005
**Added:** 2026-07-11 (STORY-164 AC-164-005)

---

## Background

STORY-160 (wave-72) introduced a BREAKING JSON change: enum casing from PascalCase to
lowercase/snake_case, plus a `schema_version` envelope. The implementation was correctly
delivered and tested against the story's own test suite. However, 13 holdout scenarios
in `.factory/holdout-scenarios/` had stale `expected_output` fields hard-coded to the old
enum names and structure. None were identified during per-story delivery.

The 13 scenarios -- HS-021/024/032/033/034/035/050/054/059/064/065/074/075 -- were repaired
by the product-owner at the wave-72 integration gate holdout re-evaluation step, a
significant unplanned gate-time work item. The root cause: no delivery-protocol step
required a sweep of holdout-scenario expectations against the new output format. Holdout
scenarios live in `.factory/holdout-scenarios/`, outside the story's own test suite, so
the standard per-story TDD cycle did not cover them.

Source: `.factory/cycles/wave-72/lessons.md` Lesson 2 (lines 46-79), tag
PG-W72-BREAKING-HOLDOUT-SWEEP.

---
```

---

## (c) Verification Grep — holdout-expectations-sweep and PG-W72-BREAKING-HOLDOUT-SWEEP

```
$ grep -n "holdout-expectations-sweep\|PG-W72-BREAKING-HOLDOUT-SWEEP" \
    $REPO_ROOT/.factory/maintenance/breaking-change-delivery-protocol.md
3:**Policy reference:** PG-W72-BREAKING-HOLDOUT-SWEEP
27:PG-W72-BREAKING-HOLDOUT-SWEEP.
57:4. **Record `holdout-expectations-sweep: COMPLETE`** in the story's delivery checklist
61:this protocol (PG-W72-BREAKING-HOLDOUT-SWEEP). The integration gate holdout re-evaluation
69:A story PR opened without `holdout-expectations-sweep: COMPLETE` for an in-scope story:
94:| Tag | PG-W72-BREAKING-HOLDOUT-SWEEP |
100:- **PG-W72-BREAKING-HOLDOUT-SWEEP:** Root process-gap tag (wave-72 Lesson 2, 2026-07-09).
```

Both strings are present in the protocol document — file exists with the required content.

---

## Result

PASS — all three verification checks pass:
1. `test -f .factory/maintenance/breaking-change-delivery-protocol.md` — file exists (factory-artifacts branch).
2. Grep emits non-empty output for `holdout-expectations-sweep` and `PG-W72-BREAKING-HOLDOUT-SWEEP`.
3. CLAUDE.md row is present at line 250 with exact AC-164-005(d) wording.
