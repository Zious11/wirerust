# AC-165-003 Demo Evidence: Delivery-Doc Currency Protocol

**AC:** AC-165-003 (traces to PG-W74-DELIVERY-DOC-CURRENCY)
**Story:** STORY-165 v1.5
**Date:** 2026-07-13
**Method:** Terminal capture — factory-artifacts branch, main repo cwd

---

## AC Restated

A new maintenance artifact `.factory/maintenance/delivery-doc-currency-protocol.md`
codifies the delivery-doc currency sweep as a mandatory wave-gate-entry step. Scope trigger:
once per wave, before the first adversarial pass of the wave gate begins (per-story Step-4.5
passes explicitly out of scope — F-S165P6-001 disambiguation). Mandatory steps: status loci
check, tense audit, demo-evidence currency notes. A `**Currency sweep: COMPLETE**` record is
required before the first adversarial pass.

---

## Command 1: File Existence Check

```
test -f .factory/maintenance/delivery-doc-currency-protocol.md && echo "FILE EXISTS"
```

Output:

```
FILE EXISTS
```

---

## Command 2: AC Verification Grep

```
grep -n "currency sweep\|PG-W74-DELIVERY-DOC-CURRENCY\|tense audit" \
  .factory/maintenance/delivery-doc-currency-protocol.md
```

Output (selected lines):

```
3:**Policy reference:** PG-W74-DELIVERY-DOC-CURRENCY
28:(Pass 13) illustrates that staleness comes in layers. A pre-gate currency sweep catches
31:Root cause: no explicit wave-gate-entry step required a full currency sweep of delivery
36:terminology in wave-gate-entry documentation). The currency sweep defined in this
128:A wave gate opened without completing the currency sweep:
154:- **PG-W74-DELIVERY-DOC-CURRENCY:** Root process-gap (wave-74 gate Passes 1 and 13,
```

Both commands succeed: file exists, grep emits non-empty output containing
"PG-W74-DELIVERY-DOC-CURRENCY" and "currency sweep". Note: "tense audit" does not appear as
a space-separated phrase; the file uses "tense-audit" (hyphen) at line 23. The grep emits
non-empty output via the other two matched patterns, satisfying the AC verification condition
("grep emits non-empty output").

---

## Wave-Gate-Entry Scoping (F-S165P6-001)

The original scope language "before any adversarial convergence pass begins" was
disambiguated in wave-75 Pass 6 (F-S165P6-001) to: "before the first adversarial pass of
the wave gate begins (per-story Step-4.5 passes NOT in scope)." This prevents the protocol
from being misread as applying to every per-story adversarial pass, which would be
operationally burdensome and is not the intent.

```
grep -n "F-S165P6-001" .factory/maintenance/delivery-doc-currency-protocol.md
```

Output:

```
171:| F-S165P6-001 | 2026-07-13 | Scope trigger wording disambiguated at all loci: "before any adversarial convergence pass begins" → "before the first adversarial pass of the wave gate begins (per-story Step-4.5 passes NOT in scope)". Clarifying sentence added to Scope section. Lines 71, 110, 116 (original numbering) qualified with "of the wave gate". Sibling locus in STORY-165.md AC-165-003(a) fixed in same burst per DF-SIBLING-SWEEP-001. |
```

The sibling-locus fix (STORY-165.md AC-165-003(a)) was applied in the same burst per
DF-SIBLING-SWEEP-001.

---

## Mandatory Steps Confirmed

The protocol covers all three mandatory sweep steps from AC-165-003(b):
1. **Status loci check** — verify frontmatter `status:`, body header, and STORY-INDEX cell agree.
2. **Tense audit** — scan for present-tense references to pre-delivery state (e.g., "Current
   gate implementation") and reframe as historical.
3. **Demo-evidence currency notes** — review demo-evidence artifacts for superseded counts or
   behavioral claims; add currency notes per the story-164 pattern.

The currency sweep record requirement (`**Currency sweep: COMPLETE (YYYY-MM-DD)**`) and the
non-conformance consequence (first adversarial pass must verify record exists) are documented
in the protocol.

---

## Result

PASS — `.factory/maintenance/delivery-doc-currency-protocol.md` exists, carries the
policy reference PG-W74-DELIVERY-DOC-CURRENCY, "currency sweep" in the body, scope scoped
to wave-gate-entry only (F-S165P6-001), and a Correction Record for the scope disambiguation.
File resides on the factory-artifacts branch.

[Currency note — 2026-07-13: Evidence captured from the factory-artifacts branch tree
at `/Users/zious/Documents/GITHUB/wirerust/.factory/maintenance/`, pre-burst-commit,
factory state as of wave-75 adversarial convergence completion (3/3 clean streak).
F-S165P6-001 is the most recent correction; the scope wording is current as of this capture.
The CLAUDE.md Project References row for this file (worktree commit 9ae8b04, line 252)
describes the scope as "mandatory wave-gate-entry sweep — status loci, tense audit,
demo-evidence currency notes — before first adversarial pass of the wave gate," which is
accurate and consistent with the disambiguated scope.]

[Post-merge currency note — 2026-07-13: STORY-165 delivered (PR #398 squash-merged
fa646ed, 2026-07-13). Factory burst committed. File content on factory-artifacts branch
unchanged since capture; scope wording and CLAUDE.md row remain accurate post-delivery.]
