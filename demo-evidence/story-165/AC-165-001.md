# AC-165-001 Demo Evidence: Bin-Selftest CI Wiring

**AC:** AC-165-001 (traces to PG-W74-CI-BIN-SELFTEST)
**Story:** STORY-165 v1.5
**Date:** 2026-07-13
**Method:** Terminal capture — worktree `ci/story-165-bin-selftest` @ commit 9ae8b04

---

## AC Restated

`.github/workflows/ci.yml` gains a `bin-selftest` CI job that runs
`bin/test_validate_citations.py` (22 tests) and `bin/test_changelog_gate_content.py`
(10 tests) following the structural pattern of the `green-doc-tense-gate` job.
The action-pin-gate must continue to pass.

---

## Command 1: AC Verification Grep (ci.yml)

```
cd /Users/zious/Documents/GITHUB/wirerust/.worktrees/story-165
grep -n "bin-selftest\|test_validate_citations\|test_changelog_gate_content" \
  .github/workflows/ci.yml
```

Output:

```
466:  # STORY-164 (wave-74) delivered bin/test_validate_citations.py (22 tests) and
467:  # bin/test_changelog_gate_content.py (10 tests) without CI wiring. This job closes that gap
469:  bin-selftest:
470:    name: Bin selftest suite (test_validate_citations + test_changelog_gate_content)
477:      - name: Run bin/test_validate_citations.py (22 tests)
478:        run: python3 bin/test_validate_citations.py
479:      - name: Run bin/test_changelog_gate_content.py (10 tests)
480:        run: python3 bin/test_changelog_gate_content.py
```

Non-empty output — job name and both run commands present. AC(a) structure confirmed.

---

## Command 2: Run bin/test_validate_citations.py (22 tests)

```
cd /Users/zious/Documents/GITHUB/wirerust/.worktrees/story-165
python3 bin/test_validate_citations.py
```

Output:

```
test_T01_valid_line_citation_passes:
  [PASS] T01 valid single-line citation: exit=0, out='PASS: 1 citations verified'

test_T02_valid_range_citation_passes:
  [PASS] T02 valid range citation: exit=0, out='PASS: 1 citations verified'

test_T03_nonexistent_file_rejected:
  [PASS] T03 nonexistent file rejected: exit=1

test_T04_out_of_range_single_line_rejected:
  [PASS] T04 out-of-range single line rejected: exit=1

test_T05_out_of_range_range_endpoint_rejected:
  [PASS] T05 out-of-range range endpoint rejected: exit=1

test_T06_comments_and_blanks_ignored:
  [PASS] T06 comments and blank lines ignored: exit=0, out='PASS: 1 citations verified'

test_T07_empty_input_passes:
  [PASS] T07 empty input: exit=0, out='PASS: 0 citations verified'

test_T08_invalid_range_start_gt_end:
  [PASS] T08 invalid range (start > end) rejected: exit=1

test_T09_bad_argument_exits_2:
  [PASS] T09 nonexistent citations file → exit 2: exit=2

test_T10_multiple_valid_citations_count:
  [PASS] T10 multiple valid citations: exit=0, out='PASS: 3 citations verified'

test_T11_mixed_valid_and_invalid:
  [PASS] T11 mixed valid+invalid → FAIL with count: exit=1

test_T12_malformed_line_reported:
  [PASS] T12 malformed citation reported: exit=1

test_T13_zero_line_number_rejected:
  [PASS] T13 zero line number rejected: exit=1

test_T14_zero_range_start_rejected:
  [PASS] T14 zero range start rejected: exit=1

test_T15_malformed_counts_in_fail_denominator:
  [PASS] T15 malformed counted in FAIL denominator: exit=1

test_T16_absolute_path_rejected:
  [PASS] T16 absolute path rejected as OUTSIDE REPO: exit=1

test_T17_parent_escape_rejected:
  [PASS] T17 parent-escape path rejected as OUTSIDE REPO: exit=1

test_T18_non_utf8_citations_file_exits_2:
  [PASS] T18 non-UTF-8 citations file → exit 2: exit=2

test_T19_unreadable_citations_file_exits_2:
  [PASS] T19 unreadable file → exit 2: exit=2

test_T20_non_utf8_stdin_exits_2:
  [PASS] T20 non-UTF-8 stdin → exit 2: exit=2

test_T21_directory_target_not_a_file:
  [PASS] T21 directory target → NOT A FILE, exit 1: exit=1

test_T22_unreadable_target_file:
  [PASS] T22 unreadable target → UNREADABLE, exit 1: exit=1

============================================================
Results: 22 passed, 0 failed
All tests passed.
```

---

## Command 3: Run bin/test_changelog_gate_content.py (10 tests)

```
cd /Users/zious/Documents/GITHUB/wirerust/.worktrees/story-165
python3 bin/test_changelog_gate_content.py
```

Output:

```
test_content_lines_variable_present:
  [PASS] CONTENT_LINES variable present in changelog-gate-check

test_changelog_diff_variable_present:
  [PASS] CHANGELOG_DIFF variable present in changelog-gate-check

test_whitespace_only_message_present:
  [PASS] whitespace-only FAIL message present in changelog-gate-check

test_content_line_pass_message_present:
  [PASS] content line PASS message present in changelog-gate-check

test_grep_filter_chain_present:
  [PASS] grep section-header filter (^+##) present in changelog-gate-check

test_B01_real_content_line_pass:
  [PASS] B01 real content line → exit 0 PASS: exit=0, out='PASS: CHANGELOG.md updated with 1 content line(s).'

test_B02_blank_only_touch_fail:
  [PASS] B02 blank-only touch → exit 1 with whitespace-only: exit=1

test_B03_section_header_only_add_fail:
  [PASS] B03 section-header-only add → exit 1 FAIL: exit=1

test_B04_deletions_only_fail:
  [PASS] B04 deletions-only → exit 1 FAIL: exit=1

test_B05_exec_bit_direct_invocation:
  [PASS] B05 direct invocation (no bash prefix): exit=0

============================================================
Results: 10 passed, 0 failed
All tests passed.
```

---

## Command 4: Git Log (worktree @ 9ae8b04)

```
cd /Users/zious/Documents/GITHUB/wirerust/.worktrees/story-165
git log --oneline -3
```

Output:

```
9ae8b04 docs: qualify currency-sweep trigger as wave-gate-entry in CLAUDE.md row (F-S165P6-001)
61b60cd docs: register STORY-165 maintenance mandates in CLAUDE.md Project References (F-S165P4-003)
b4b7435 ci: add bin-selftest job running bin/test_validate_citations.py + bin/test_changelog_gate_content.py (STORY-165 AC-165-001)
```

---

## Command 5: Git Diff Stat (d6e3be8..HEAD)

```
cd /Users/zious/Documents/GITHUB/wirerust/.worktrees/story-165
git diff --stat d6e3be8..HEAD
```

Output:

```
 .github/workflows/ci.yml | 18 ++++++++++++++++++
 CLAUDE.md                |  2 ++
 2 files changed, 20 insertions(+)
```

Two files modified on the develop track: `ci.yml` (+18 lines, the `bin-selftest` job) and
`CLAUDE.md` (+2 lines, the Project References rows for the two new maintenance docs per
F-S165P4-003).

---

## Command 6: CLAUDE.md Amendment Rows (F-S165P4-003)

```
cd /Users/zious/Documents/GITHUB/wirerust/.worktrees/story-165
grep -n "pr-description-row-verify-mandate\|delivery-doc-currency-protocol" CLAUDE.md
```

Output:

```
251:| `.factory/maintenance/pr-description-row-verify-mandate.md` | PR-description test-evidence row-verify + aggregate-count cross-check mandate (PG-W74-PRDESC-ROW-VERIFY; pr-reviewer/pr-manager must row-verify ≥3 per-test entries and cross-check claimed counts against actual CI output) |
252:| `.factory/maintenance/delivery-doc-currency-protocol.md` | Delivery-doc currency sweep (PG-W74-DELIVERY-DOC-CURRENCY; mandatory wave-gate-entry sweep — status loci, tense audit, demo-evidence currency notes — before first adversarial pass of the wave gate) |
```

Both Project References rows present at lines 251–252. Amendment F-S165P4-003 confirmed at
both loci (ci.yml + CLAUDE.md).

---

## Result

PASS — The `bin-selftest` job is present in `.github/workflows/ci.yml` at lines 469–480 with
the correct name, both run steps, and the structural pattern of `green-doc-tense-gate`. Both
test suites pass locally (22/22 and 10/10). CLAUDE.md carries the two Project References rows
for the new maintenance docs. Commit history: b4b7435 (ci.yml), 61b60cd (CLAUDE.md),
9ae8b04 (CLAUDE.md qualification for F-S165P6-001).

[Currency note — 2026-07-13: Evidence captured in worktree `ci/story-165-bin-selftest` at
commit 9ae8b04, pre-merge, factory state pre-burst-commit. Line numbers in ci.yml (466–480)
reflect the worktree state at this SHA. After merge to develop the line numbers may shift as
other CI jobs are added; the verification grep is string-based and remains valid post-merge.
The `git diff --stat d6e3be8..HEAD` output reflects the two-file develop-track diff; the
factory-artifacts track (AC-165-002, AC-165-003, AC-165-004) is not captured in this diff
as those files reside on the factory-artifacts branch.]

[Post-merge currency note — 2026-07-13: PR #398 squash-merged to develop as fa646ed
(2026-07-13). The bin-selftest job is now live on develop CI — first run green (13/13 CI
jobs green including bin-selftest, per STORY-165 v1.6 changelog). Factory burst committed.
All claims in the preceding currency note remain accurate; the string-based verification
grep confirmed valid post-shift.]
