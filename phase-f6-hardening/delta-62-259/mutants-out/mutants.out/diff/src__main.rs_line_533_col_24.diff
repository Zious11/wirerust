--- src/main.rs
+++ replace == with != in resolve_targets
@@ -525,17 +525,17 @@
     }
     if target.is_dir() {
         let mut files = Vec::new();
         for entry in std::fs::read_dir(target)? {
             let entry = entry?;
             let path = entry.path();
             if path.is_file()
                 && let Some(ext) = path.extension()
-                && ext == "pcap"
+                && ext != /* ~ changed by cargo-mutants ~ */ "pcap"
             {
                 files.push(path);
             }
         }
         files.sort();
         return Ok(files);
     }
     anyhow::bail!("Target not found: {}", target.display());
