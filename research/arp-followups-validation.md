# ARP Security Analyzer — Follow-Up Findings Validation (DF-VALIDATION-001)

**Date:** 2026-06-16
**Validator:** vsdd-factory:research-agent
**Scope:** Open follow-up findings from the ARP analyzer feature cycle (STORY-111..115, F1–F7).
**develop HEAD at validation:** e37ec38 (read against working tree; STATE.md references PR #240 merge @ 7c0f453 for MITRE counts).
**Policy gate:** Per DF-VALIDATION-001, no GitHub issue may be filed from an unvalidated finding. Each item below is confirmed GENUINE / not against current code + specs before a disposition is assigned.

---

## Validation Table

| # | Item | GENUINE? | Severity / User Impact | Effort | DISPOSITION | Recommended approach + citations |
|---|------|----------|------------------------|--------|-------------|----------------------------------|
| 1a | FU-F6-KANI-CLEANUP — VP-024 `proof_file_hash: null` | **GENUINE** | Low (governance/provenance only; not a runtime defect). Impact: VP-024 is the only F6-locked VP lacking a proof-integrity hash, breaking the cross-VP convention. | S–M | **FILE-ISSUE** (post-release) | Real inconsistency: VP-020/022/011/019/012/005 all populate `proof_file_hash` as the SHA-256 of the single proof file (VP-022 spec line 305: "`proof_file_hash` is the SHA-256 of the proof harness file `src/analyzer/modbus.rs`"). VP-024 is null because its proofs span **two** files (`src/decoder.rs` kani_proofs + `src/analyzer/arp.rs` kani_proofs) — the single-file convention does not directly apply, which is exactly why it was deferred. Right method: define a deterministic multi-file digest — SHA-256 over the concatenation of the two `#[cfg(kani)] mod kani_proofs` module byte-ranges (or, simpler and more robust to line moves, SHA-256 of each whole file concatenated in a fixed declared order, mirroring the `bin/compute-input-hash` LF-normalization discipline already canonical in this repo per CLAUDE.md). Requires a VP withdrawal/re-lock since VP-024 is `verification_lock: true` — hence post-release. |
| 1b | FU-F6-KANI-CLEANUP — CR-001: `verify_extract_arp_frame_safety` lacks `kani::cover!` vacuity guard | **GENUINE but COSMETIC** | Negligible. The proof is sound (panic-freedom holds regardless of branch reachability). | XS | **DROP** (optionally fold a 1-line `kani::cover!` into the 1a re-lock if it happens) | Confirmed: the harness (decoder.rs:616-634) is a no-assert panic-freedom proof over a symbolic 28-byte buffer; it calls `extract_arp_frame` inside `if let Ok(...)` and discards the result. A `kani::cover!` is **NOT necessary** for soundness of a panic-only harness — the implicit obligation (no unwind on any explored path) is non-vacuous on its own; `kani::cover!` is Kani's *optional* reachability/coverage tool, not a mandated vacuity guard, and Kani docs do not require it on panic-only harnesses [Kani reference: model-checking.github.io/kani — `kani::cover` documented as reachability/coverage, not mandatory]. It is only "needed" if you want to *certify* the Ok arm is exercised. Note: the sibling harnesses `..._eth_ipv4_correctness` and `..._none_on_bad_size` (which DO contain assertions) already carry `kani::cover!` guards — so the cover-bearing harnesses already prove the same 28-byte Ok arm is reachable. Adding one to the safety harness is pure belt-and-suspenders. Low-cost if bundled, but not worth a standalone change. |
| 1c | FU-F6-KANI-CLEANUP — CR-002: `insert_binding_lru_array` doc lacks explicit `cap > 0` precondition | **GENUINE but COSMETIC** | Negligible. `#[cfg(any(kani, test))]` helper; all callers pass cap=8. | XS | **DROP** (or fold into a doc-chore) | Confirmed: function at arp.rs:290-297 is test/proof-gated, not public production API. Rust API Guidelines (rust-lang.github.io/api-guidelines, C-FAILURE) urge documenting panic/precondition contracts **for public APIs**; for an internal proof-only helper with a fixed nonzero caller, omitting `cap > 0` is a minor/cosmetic omission, not a defect. The doc already states "`N` is the array capacity (must be >= `cap`)" — adding "requires `cap > 0`" is a one-line nicety. Cheapest to fold into 3 (doc-debt PR) if actioned at all. |
| 2 | FU-ARP-QINQ-MACSEC-TEST (O-2): no targeted QinQ/MACsec decoder fixture | **GENUINE** | Low–Medium. The VLAN-offset peek (decoder.rs:315-348) is correct-by-construction via `link_exts.iter().map(header_len).sum()`, but only **single 802.1Q** is unit-tested (tests/bc_2_16_d078_vlan_offset_tests.rs: 4 tests — VLAN/non-VLAN × benign/malformed). QinQ (+8) and MACsec (variable) offsets are exercised only by F6 fuzz (16.2M execs, 0 crashes) — random, not asserting correct malformed-vs-truncation classification. | M | **FILE-ISSUE** (post-release); fuzz is sufficient interim safety coverage | Worth adding 2–3 targeted fixtures: (i) QinQ double-tag (0x88a8 outer + 0x8100 inner) truncated benign ARP — assert NO false D11; (ii) QinQ truncated malformed (hlen!=6 at offset 22) — assert D11; optionally (iii) MACsec + ARP. etherparse 0.20.x **does** support both: `PROVIDER_BRIDGING` (0x88a8) and `VLAN_TAGGED_FRAME` (0x8100) are listed supported EtherTypes, and MACsec (802.1AE) is a first-class supported protocol with `LaxLinkExtSlice` / `LaxMacsecPayloadSlice` types; QinQ parses to two 4-byte `link_exts` entries (sum=8) and `header_len()` reports each extension's on-wire length [docs.rs/etherparse SlicedPacket; github.com/JulianSchmid/etherparse; lib.rs/crates/etherparse — 0.20.1, Apr 2026]. NOTE: the etherparse evidence for exact field/method names was partly *inferred* from docs (variant names `Vlan`/`Macsec` and `header_len()` not quoted verbatim) — which is itself the strongest argument FOR a fixture: a unit test empirically pins the inferred behavior. Fuzz (16.2M/0) is adequate as the **interim panic-freedom** guarantee; it does not assert classification correctness. Not release-blocking. |
| 3 | FU-REPO-WIDE-DOC-DEBT: stale RED-gate prose | **GENUINE — larger than estimated** | Low (doc accuracy only; tests are GREEN/passing). Misleading to future readers: "RED: stub not wired" on tests that pass. | M | **FILE-ISSUE** → standalone `docs:`/`test:` chore PR (post-release) | Confirmed and scoped wider than the STATE.md note: live grep shows **71 occurrences across 5 test files** (dnp3_f5_remediation=51, bc_2_15_110_dnp3_dispatcher=8, dnp3_detection=6, cli_story_087=4, dnp3_flow_state=2) **plus ~13 occurrences in src/analyzer/arp.rs** (e.g. arp.rs:2755 `// RED: will fail if stub not wired` on a passing test; lines 2608–3170). All are GREEN tests carrying RED-phase tense. Right approach: a single mechanical chore PR rewriting RED→GREEN regression-guard tense (precedent exists: commit dcdbf95 "docs(STORY-115): rewrite F-1 regression-test prose RED→GREEN"). Do NOT bundle into a feature story. No code change, low risk, but touches many files — schedule on its own. |
| 4 | FU-JSON-CASING: enums PascalCase in JSON vs UPPERCASE in Display | **GENUINE inconsistency, NOT a defect** | Low. JSON envelope is internally self-consistent PascalCase (`"High"`, `"Possible"`, `"Anomaly"`); Display renders UPPERCASE for the terminal only. SIEMs consume JSON, not the TTY stream. | M (cross-cutting governed change) | **FILE-ISSUE** (post-release, governed); **NOT FIX-NOW** | Validated against security-telemetry conventions: PascalCase-in-JSON + UPPERCASE-on-console is a **cosmetic internal inconsistency, not a functional defect** — SIEMs parse JSON independently of console output and treat string values per ingestion rules. Ecosystem norm for enum-like severity/category fields is **lowercase or snake_case**: Suricata EVE uses numeric severity + lowercase `event_type`; ECS `log.level` examples use lowercase ("info"/"error"); OCSF uses numeric `severity_id`; none mandate UPPERCASE in JSON [forum.suricata.io eve-json severity; elastic.co/docs ECS log fields; ocsf.io; docs.suricata.io eve-json-format]. **Two divergent targets exist** — resolve the conflict before filing: (a) STATE.md FU-JSON-CASING entry proposes aligning serde to **UPPERCASE** (`SCREAMING`) to match the existing `Display`/BC-2.09.004 contract — internally coherent, zero churn for current consumers, but *atypical* vs ecosystem; (b) external best-practice favors **lowercase/snake_case** (`#[serde(rename_all="lowercase")]` for Confidence/Verdict, `"snake_case"` for ThreatCategory) for ECS/OCSF alignment [serde.rs rename_all]. Recommendation: since wirerust JSON is already internally consistent, treat this as an **improvement, not a defect**; if actioned, prefer the ecosystem-aligned lowercase/snake_case AND document it as a breaking JSON-schema change (touches BC-2.09.004, BC-2.11.001, ADR-0003). Either way: governed, post-release, never bundled into a feature story. |
| 5 | FU-BC-2.10.007-MARKER: PLANNED marker for `technique_tactic` resolution | **GENUINE — marker IS stale** | Low (spec-accuracy/drift only). | XS–S | **FIX-NOW** (spec hygiene, before v0.7.0) | Confirmed stale. `technique_tactic` (src/mitre.rs:199-201) fully resolves T0830→LateralMovement and T1557.002→CredentialAccess; the Kani harness `verify_all_seeded_ids_resolve` (mitre.rs:267-273) proves all 25 seeded IDs resolve. BUT BC-2.10.007 body still reads (line 44/50/58/128): "23 seeded technique IDs", "PLANNED — implemented in STORY-114... src/mitre.rs remains at SEEDED=23 until STORY-114 lands". Its **siblings already de-PLANNED**: BC-2.10.005 v1.11 and BC-2.10.008 v1.13 carry post-merge governance updates to 25/17. BC-2.10.007 was missed in that sweep → it is now internally inconsistent with .005/.008. Right approach: PO update raising 23→25 throughout postconditions/invariants/VP table, add the T0830/T1557.002 tactic rows, remove the PLANNED marker (mirror the .005/.008 v1.11/v1.13 edits). Low effort; should be done as spec hygiene before tagging v0.7.0. |
| 6 | FU-STORM-NEW-ATTR: doc mis-attributes `storm_rate` to STORY-114 at ~line 272 | **NOT GENUINE as described — already correct** | None | — | **DROP** (verify-and-close) | Could not reproduce the claimed mis-attribution. arp.rs:272 is inside the VP-024 `insert_binding_lru_array` doc (no storm_rate text). Every `storm_rate` doc reference is **correctly** attributed to STORY-115: line 340-345 ("STORY-115 implements D3 storm detection… `--arp-storm-rate` is wired"), line 359-362, line 377, line 394-396 ("Overridable via `--arp-storm-rate` (STORY-115)"). The only STORY-114 mentions near `storm_rate` are factually correct ("STORY-114 extends `new(spoof_threshold, storm_rate)`" — the *signature* extension genuinely landed in STORY-114; STORY-115 added the *detection logic*). Finding appears already-resolved (likely fixed in the STORY-115 burst) or mis-located. Close as not-applicable; no issue to file. |
| 7 | BC-2.10-COUNT-POSTMERGE: BC-2.10.005/.008 "23/15" → "25/17" | **NOT GENUINE — already resolved for .005/.008** | None (markers already updated) | — | **DROP** for .005/.008; **see item 5** for .007 | Code confirmed: `SEEDED_TECHNIQUE_ID_COUNT = 25` (mitre.rs:357), 25 entries in `SEEDED_TECHNIQUE_IDS`, 17 entries in `EMITTED_IDS` (mitre.rs:228-250). BC-2.10.005 title now reads "25 Total" and v1.11 changelog ("PLANNED marker resolved… SEEDED=25/EMITTED=17 confirmed"); BC-2.10.008 v1.13 same. So the "23/15→25/17" TODO is **already done** for these two. The STATE.md `BC-2.10-COUNT-POSTMERGE` line (still "OPEN") is itself stale bookkeeping — recommend marking it RESOLVED. The genuinely-stale residual is BC-2.10.007 (item 5), which was not swept. |

**Severity legend:** Negligible < Low < Medium < High. **Effort:** XS (≤15 min) < S (<1h) < M (≤half day).

---

## Summary & Recommendations

### FIX-NOW (before v0.7.0) — spec hygiene only, no code changes
- **Item 5 (FU-BC-2.10.007-MARKER):** Update BC-2.10.007 to remove the stale "23 seeded / PLANNED" marker and bring it to 25 seeded with the T0830/T1557.002 tactic rows, matching its already-updated siblings BC-2.10.005 (v1.11) and BC-2.10.008 (v1.13). This is a one-file PO edit closing a real spec-drift inconsistency before the release tag. (XS–S effort.)

### FILE GITHUB ISSUE (post-release)
- **Item 1a (proof_file_hash):** Define a canonical **multi-file** SHA-256 method for VP-024 (its proofs span decoder.rs + arp.rs, unlike single-file VP-020/022/...) and populate via a VP re-lock. Reuse the repo's existing LF-normalized concatenation discipline.
- **Item 2 (QinQ/MACsec fixtures):** Add 2–3 targeted decoder fixtures (QinQ benign-truncated, QinQ malformed, optional MACsec). Empirically pins etherparse 0.20 behavior that is currently only *inferred from docs* and *fuzz-covered*. Fuzz (16.2M/0) is adequate interim panic-freedom coverage — not release-blocking.
- **Item 3 (repo-wide doc debt):** Standalone `docs:`/`test:` chore PR rewriting 71 stale "RED: stub not wired" occurrences across 5 test files + ~13 in arp.rs to GREEN regression-guard tense. Larger than the original 13-file estimate. Do NOT bundle into a feature story.
- **Item 4 (FU-JSON-CASING):** Governed cross-cutting JSON-contract change. Current state is an internally-consistent improvement opportunity, NOT a defect. Resolve the UPPERCASE-vs-lowercase target conflict first (STATE.md proposes UPPERCASE for Display-parity; ecosystem best-practice favors lowercase/snake_case for ECS/OCSF). If actioned, treat as a documented breaking JSON-schema change touching BC-2.09.004 / BC-2.11.001 / ADR-0003.

### DROP (not a real issue / already resolved)
- **Item 1b (CR-001 kani::cover!):** Panic-only harness is sound without it; sibling assert-bearing harnesses already cover the same Ok-arm reachability. Optionally fold one line into the 1a re-lock.
- **Item 1c (CR-002 cap>0 doc):** Cosmetic for a test/proof-only helper; optionally fold into the item-3 doc PR.
- **Item 6 (FU-STORM-NEW-ATTR):** Not reproducible — every `storm_rate` doc reference already correctly attributes to STORY-115; STORY-114 mentions are factually correct (signature extension). Verify-and-close.
- **Item 7 (.005/.008 counts):** Already resolved to 25/17 (BC-2.10.005 v1.11, BC-2.10.008 v1.13, code `SEEDED_TECHNIQUE_ID_COUNT=25`). Recommend marking the STATE.md `BC-2.10-COUNT-POSTMERGE` line RESOLVED. The only un-swept residual is BC-2.10.007 → handled under item 5.

### Net release-readiness assessment
Nothing here blocks v0.7.0 functionally. The single pre-tag action is **item 5** (a one-file spec consistency fix). Items 1a, 2, 3, 4 are legitimate post-release GitHub issues. Items 1b, 1c, 6, 7 should be dropped/closed (with item 7's STATE.md bookkeeping line marked resolved).

---

## Research Methods

| Tool | Queries | Purpose |
|------|---------|---------|
| **Perplexity perplexity_research (PRIMARY)** | 3 | (1) Kani vacuity / `kani::cover!` necessity on panic-only harnesses + Rust API-guideline precondition-doc conventions [item 1b/1c]; (2) JSON casing conventions for security-tool/SIEM consumers + serde `rename_all` [item 4]; (3) etherparse 0.20 QinQ/MACsec `link_exts`/`header_len()` parsing specifics [item 2]. |
| Perplexity perplexity_reason | 0 | — |
| Perplexity perplexity_search | 0 | — |
| Perplexity perplexity_ask | 0 | — |
| Context7 | 0 | — (etherparse covered by perplexity_research with docs.rs citations) |
| Tavily * | 0 | — |
| WebFetch | 0 | — |
| WebSearch | 0 | — |
| Read | 8 | findings.rs, decoder.rs, arp.rs (4 ranges), mitre.rs (3 ranges), BC-2.10.007.md, VP-022 spec, tool-result files |
| Grep | 12 | RED-prose scope, storm_rate attribution, SEEDED/EMITTED counts, technique_tactic, QinQ/MACsec presence, proof_file_hash convention, BC markers |
| Training data | 1 area | Kani vacuity reasoning — flagged: the Kani research call self-reported it relied on training-data knowledge (no live retrieval), so item 1b's Kani-doc claim is corroborated by reasoning + general Kani API knowledge rather than a freshly-fetched doc page. Conclusion (cover! optional for panic-only harnesses) is well-established and cross-checked against the repo's own sibling harnesses. |

**Total MCP tool calls:** 3 (all `perplexity_research`, reasoning_effort=medium).
**Training data reliance:** low–medium — items 4 and 2 are web-cited (Suricata/ECS/OCSF/serde; docs.rs/etherparse/github); item 1b's Kani guidance is the one area leaning on model knowledge (flagged above). All repo-state claims (counts, marker staleness, file scope, code behavior) are verified directly against the working tree, not inferred.
